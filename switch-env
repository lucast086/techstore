#!/bin/bash
# Convenience wrapper for switch_env.sh that can be called from anywhere

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Call the actual script
"$SCRIPT_DIR/scripts/switch_env.sh" "$@"
