[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -c \"import os; print(f'PORT={os.environ.get(\'PORT\', \'NOT SET\')}')\" && uvicorn src.app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
